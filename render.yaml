services:
  - type: web
    name: game-chaos-hub
    env: node
    buildCommand: pnpm install --include=dev && pnpm build
    startCommand: node dist/index.js
    healthCheckPath: /
    plan: free
    region: singapore